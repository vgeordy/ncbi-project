import "./spinner.styles.css";

// Spinner Component
// This component is used to create load spinner while data is being fetched from API.
export default function Spinner() {
  return <div className="spinner"></div>;
}
